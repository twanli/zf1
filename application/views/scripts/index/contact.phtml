<?php
    $this->form->setAction($this->url());
    echo $this->form;
    if ($this->sent != null) {
        if($this->sent == 1) {
            echo "<span>Message was sent successfully.</span>";    
        } else {
            echo "<span>There is an error, check your configuration file in
                 configs/config.ini file .</span>";    
        }
        
    }
